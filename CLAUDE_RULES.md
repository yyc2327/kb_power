# Claude Code 工程规则

> **适用**：所有代码生成、重构、评审任务  
> **目标**：可读、可维护、生产就绪

---

## 一、核心哲学

- 代码是写给人看的，顺便给机器执行
- Simple > Clever | Explicit > Implicit | Correctness > Performance

---

## 二、通用强制规则

### 命名

- 变量：`userCount` ✅ `cnt` ❌
- 函数：动词开头，无副作用
- 布尔：`isValid`、`hasAccess`
- 常量：全大写下划线

### 函数

- ≤ 30 行
- ≤ 3 个参数（超则封装对象）
- 单一职责，无隐式修改

### 类与结构

- 单类单责（SRP）
- 组合 > 继承
- 依赖抽象（DIP）
- 禁止 God Class

---

## 三、SOLID 速记

| 原则 | 要求 |
| ---- | ---- |
| S | 一个类只为一个变化负责 |
| O | 对扩展开放，对修改关闭 |
| L | 子类可替换父类 |
| I | 接口最小化 |
| D | 依赖抽象，不依赖实例 |

---

## 四、错误处理

- 不吞异常
- 区分预期错误 / 系统异常
- 结构化错误信息
- 所有外部输入必须校验

---

## 五、DRY / KISS / YAGNI

- 重复 2 次 → 提函数
- 重复 3 次 → 抽象模块
- 不用模式解决 if/else 能做的事
- 不实现“以后可能用到”的功能

---

## 六、测试

- 核心逻辑 100% 覆盖
- Arrange → Act → Assert
- 含边界 + 异常路径测试

---

## 七、架构约束

### 分层架构

```
Controller → Service → Repository
```

- **Controller**：只接请求
- **Service**：纯业务逻辑
- **Repository**：只管数据访问
- **内层不依赖外层**

### React 架构

- **组件**：UI 呈现 + 用户交互
- **Hooks**：逻辑复用
- **Stores**：状态管理
- **Utils**：纯工具函数

---

## 八、Claude 行为规则

### 生成时必做

- 简述设计理由
- 标注改进点与技术债
- 给出使用示例

### 禁止

- 无注释复杂正则
- 单文件 > 100 行
- 假设环境已配置
- 使用 `any`（TS）或无类型（Python）

### 代码评审触发

当收到 `Review this code`：
1. 是否违反 SOLID
2. 是否重复代码
3. 是否有安全隐患
4. 是否过度设计
5. 是否可测试

---

## 九、语言速记

### TypeScript

- 禁 `any`，开 `strict`
- 使用 `interface` 定义数据结构
- 使用 `type` 定义联合类型、工具类型
- 导出函数带类型注解

### React

- 优先使用函数式组件和 Hooks
- 组件保持单一职责，控制在 300 行内
- 使用 TypeScript 类型注解
- 避免过度抽象，保持代码简单

---

## 十、自检清单（每次生成后自动校验）

- [ ] 命名自解释
- [ ] 函数单一职责
- [ ] 无重复代码
- [ ] 错误处理完整
- [ ] 无过度设计
- [ ] 测试覆盖核心逻辑
- [ ] 无 `any` 类型
- [ ] 文件不超过 100 行（除配置文件）

---

## 十一、项目特定规则

### 本项目补充规则

1. **组件**：优先使用 Composition 而非 Inheritance
2. **状态管理**：使用 Zustand，避免 Context Hell
3. **样式**：使用 Tailwind CSS，内联样式仅用于动态计算值
4. **类型**：所有数据结构必须在 `@/types/` 中定义
5. **Mock**：测试数据统一在 `@/mock/` 管理
6. **Git**：提交信息必须符合约定式提交规范

### 禁止事项

- ❌ 使用 `any` 类型
- ❌ 单文件超过 100 行（除配置、类型定义）
- ❌ 函数超过 30 行
- ❌ 组件超过 300 行
- ❌ 硬编码魔法数字/字符串
- ❌ 过度设计，实现“可能用到”的功能
